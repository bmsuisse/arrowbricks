[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "arrowbricks_core"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
python-source = "python"
module-name = "arrowbricks_core.arrowbricks_core"
